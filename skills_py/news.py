# skills_py/news.py

triggers = ["news", "headlines"]

def run(user_input: str) -> dict:
    # Internal planning or retrieval
    # Write to memory
    # Return a structured result
    return {
        "agent": "NewsAgent",
        "timestamp": ...,
        "summary": [...],
        "source": ...
    }
